# math.sqrt(x): Retorna a raiz quadrada de x.
# math.pow(x, y): Retorna x elevado à potência y (igual a x ** y).
# math.exp(x): Retorna e^x, onde e é a base do logaritmo natural (~2.718).
# math.log(x): Retorna o logaritmo natural de x (base e).
# math.log(x, base): Retorna o logaritmo de x na base especificada.
# math.log10(x): Retorna o logaritmo de x na base 10.
# math.log2(x): Retorna o logaritmo de x na base 2.
# math.factorial(x): Retorna x! (fatorial de x).
# math.gcd(a, b): Retorna o maior divisor comum entre a e b.
# math.lcm(a, b): Retorna o menor múltiplo comum entre a e b (Python 3.9+).
# math.ceil(x): Retorna o menor inteiro maior ou igual a x.
# math.floor(x): Retorna o maior inteiro menor ou igual a x.
# math.trunc(x): Retorna a parte inteira de x (corta a parte decimal).
# math.fabs(x): Retorna o valor absoluto de x (igual a abs(x)).
# math.copysign(x, y): Retorna x com o sinal de y.
